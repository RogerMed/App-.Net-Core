@model Entities.ToDo
@{
    ViewData["Title"] = "Remover";
}

<div class="card">
    <div class="card-header">Editar a tarefa</div>
    <div class="card-body">
       Você  deseja remover a tarefa @Model.Tarefa? <br />
        @Html.ActionLink("Sim","ConfirmaRemover", "Home", new { id = Model.id}, new { @class="btn btn-danger btn-sm"})
        @Html.ActionLink("Voltar","Index", "Home",null, new { @class="btn btn-ligth btn-sm"})
    </div>
</div>
